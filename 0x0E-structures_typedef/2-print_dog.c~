#include <stdio.h>
#include "dog.h"
#include <stdlib.h>
void print_dog(struct dog *d)
{
	/*char *name;
	float age;
	char *owner;*/

	if (d == NULL)
		return;
	if ((*d).name == NULL)
		printf("Name: (nil)\n");
	else
		printf("Name: %s\n", name);
	printf("Age: %f\n", age);
	if ((*d).owner == NULL)
		printf("Owner: (nil)\n");
	else
		printf("Owner: %s\n", owner);
}

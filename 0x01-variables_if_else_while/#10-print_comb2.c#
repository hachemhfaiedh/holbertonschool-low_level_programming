#include <stdio.h>
/**
 * main - alphabet
 * Return: (0)
 */
int main(void)
{
  int c;
  c = '0';
  for (c = '0'; c <= '100'; c++)
    {
putchar((c/10)+'0');
 putchar((i%10)+'0');
}
putchar('\n');
return (0);
}

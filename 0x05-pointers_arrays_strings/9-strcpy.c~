#include "holberton.h"
char *_strcpy(char *dest, char *src)
{
  int i = 0;
  int u;
  while (src[i] != '\0')
    {
    i++;
    }
  for (u = 0; u <= i; u++)
    {    
dest[i] = src[i];
    } 
 return (dest);
}

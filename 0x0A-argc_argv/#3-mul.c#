#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
	int i, c = 0;

	for (i = 1; i < argc; i++)
	{
		c = atoi(argv[1]) * atoi(argv[2]);
	}
	if (argc < 3)
	{
		printf("%d\n", c);
		return (0);
}
